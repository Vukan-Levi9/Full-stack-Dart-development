name: movies_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  movies_app_models:
    path: ../packages/movies_app_models
  equatable: ^2.0.5
  flutter_bloc: ^8.1.4
  fpdart: ^1.1.0
  dio: ^5.4.1
  bloc: ^8.1.3
  get_it: ^7.6.7
  flutter_secure_storage: ^9.0.0
  go_router: ^13.2.0
  flutter_launcher_icons: ^0.13.1
  web_socket_channel: ^2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  generate: true

  assets:
    - assets/images/
